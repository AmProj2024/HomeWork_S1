// import { Action } from 'rxjs/internal/scheduler/Action';
// import { createAction, props } from "@ngrx/store";

// export const Add=createAction("Add")
// export const Update=createAction("Update")
// export const Delete=createAction("Delete")
// export const Retrive=createAction("Retrive")


// import { createAction, props } from '@ngrx/store';
// import { AllProjectModule } from './Allproject.module';

// export const createProject = createAction('[Project] Create', props<{ project: AllProjectModule }>());
// export const deleteProject = createAction('[Project] Delete', props<{ projectId: string }>());
// export const updateProject = createAction('[Project] Update', props<{ project: AllProjectModule }>());
// export const retrieveProject = createAction('[Project] Retrieve', props<{ projectId: string }>());
